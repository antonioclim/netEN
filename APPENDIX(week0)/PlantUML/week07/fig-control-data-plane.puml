@startuml fig-control-data-plane
'A4 Landscape
skinparam backgroundColor white
skinparam defaultFontName Arial
skinparam defaultFontSize 11
skinparam shadowing false

title Control Plane vs Data Plane

rectangle "Control Plane (Brain)" as control #E3F2FD {
    rectangle "Routing Protocols\nOSPF, BGP, RIP" as routing #BBDEFB
    rectangle "Routing Table\nManagement" as rtable #BBDEFB
    rectangle "Network Topology\nDiscovery" as topo #BBDEFB
    
    note bottom
        Decides WHERE to forward
        * Builds routing table
        * Exchanges routing info
        * Calculates best paths
        * Runs on CPU
    end note
}

rectangle "Data Plane (Muscle)" as data #E8F5E9 {
    rectangle "Packet Forwarding\nEngine" as fwd #C8E6C9
    rectangle "Forwarding Table\n(FIB)" as fib #C8E6C9
    rectangle "Interface\nProcessing" as intf #C8E6C9
    
    note bottom
        Does actual forwarding
        * Receives packets
        * Looks up destination
        * Forwards out interface
        * Runs on ASIC/hardware
    end note
}

control -down-> data : Populates\nFIB

note as N1
    Traditional Router:
    Both planes in same device
    
    SDN:
    Control plane = Controller
    Data plane = Switch
end note

