@startuml fig-session-layer
'A4 Portrait
skinparam backgroundColor white
skinparam defaultFontName Arial
skinparam defaultFontSize 11
skinparam shadowing false

title OSI Session Layer Concepts\nLayer 5 - Dialog management

rectangle "Session Layer Functions" as funcs #F5F5F5 {
    rectangle "Dialog Control" as dialog #BBDEFB {
        rectangle "Simplex\n(one-way)" as simplex
        rectangle "Half-Duplex\n(alternating)" as half
        rectangle "Full-Duplex\n(simultaneous)" as full
    }
    
    rectangle "Synchronization" as sync #C8E6C9 {
        rectangle "Checkpoints" as cp
        rectangle "Token Passing" as token
    }
    
    rectangle "Session Management" as mgmt #FFE0B2 {
        rectangle "Establish\nMaintain\nTerminate" as est
    }
}

note right of funcs
    Modern Equivalents:
    * HTTP sessions (cookies)
    * WebSocket connections
    * TLS session resumption
    * RPC session management
end note

note bottom of funcs
    Session vs Connection:
    
    Connection: Transport-level (TCP)
    - Survives until closed
    
    Session: Application-level state
    - Can span multiple connections
    - Example: HTTP cookie session
end note

