@startuml fig-rest-levels
!theme plain
skinparam backgroundColor #FAFAFA
skinparam defaultFontName Arial
skinparam titleFontSize 18
skinparam titleFontColor #1E3A5F

title Richardson Maturity Model - REST Levels

rectangle "Level 3: Hypermedia Controls (HATEOAS)" as L3 #E8F5E9 {
  note right
    Responses contain links
    for automatic navigation
    
    **Example:**
    {
      "user": {"id": 1, "name": "John"},
      "_links": {
        "self": "/api/users/1",
        "orders": "/api/users/1/orders"
      }
    }
  end note
}

rectangle "Level 2: HTTP Verbs + Status Codes" as L2 #E3F2FD {
  note right
    Correctly uses:
    GET, POST, PUT, DELETE
    200, 201, 404, 422...
    
    **Example:**
    PUT /api/users/1 → 200 OK
    DELETE /api/users/1 → 204 No Content
  end note
}

rectangle "Level 1: Resources" as L1 #FFF3E0 {
  note right
    Different URIs for
    different resources
    
    **Example:**
    /api/users
    /api/users/1
    /api/orders
  end note
}

rectangle "Level 0: The Swamp of POX (RPC)" as L0 #FFEBEE {
  note right
    Single endpoint,
    actions in body
    
    **Example:**
    POST /api
    {"method": "getUser", "id": 1}
  end note
}

L3 -[hidden]down-> L2
L2 -[hidden]down-> L1
L1 -[hidden]down-> L0

note bottom of L3
  **Glory of REST**
  The API is truly RESTful
  Automatic capability discovery
end note

note bottom of L0
  **Most "REST" APIs**
  In reality, Level 2 is the most common
end note

@enduml
