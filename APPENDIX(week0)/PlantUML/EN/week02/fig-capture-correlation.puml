@startuml fig-capture-correlation
skinparam backgroundColor white
skinparam defaultFontName Arial
skinparam defaultFontSize 11
skinparam shadowing false
title Correlating Logs with Packet Capture

participant "Client h2" as C
participant "Server h1" as S

C -> S : SYN (Packet #1)
S --> C : SYN-ACK (Packet #2)
C -> S : ACK (Packet #3)

C -> S : PSH-ACK "Hello" (Packet #4)
S --> C : PSH-ACK "OK" (Packet #5)

legend right
App Log: Connected 14:32:15
Packet #4: timestamp matches
tshark -r demo.pcap
  -T fields -e frame.number
  -e tcp.payload
endlegend
@enduml
