# generate_docx (optional)

This folder contains a small helper to generate a DOCX lab sheet in English.

Run:
```bash
python3 generate_docx/generate_docx.py --out docs/Week7_Lab_Sheet.docx
```

If `python-docx` is missing:
```bash
python3 -m pip install python-docx
```
