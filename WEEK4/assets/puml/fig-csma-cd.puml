@startuml
skinparam backgroundColor white
skinparam shadowing false
skinparam roundcorner 12
skinparam defaultFontName Arial
skinparam defaultFontSize 14

title CSMA/CD (Ethernet half-duplex) - flow

start
:Listen to medium (Carrier Sense);
if (Medium free?) then (da)
  :Transmit frame;
  if (Collision detected?) then (da)
    :Send jamming signal;
    :Wait random backoff;
    :Retry;
  else (nu)
    :Transmission successful;
  endif
else (nu)
  :Wait until free;
endif
stop
@enduml
