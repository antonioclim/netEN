@startuml fig-pop3-vs-imap
'A4 Landscape
skinparam backgroundColor white
skinparam defaultFontName Arial
skinparam defaultFontSize 10
skinparam shadowing false

title POP3 vs IMAP Comparison

rectangle "POP3\nPort 110 (995 TLS)" as pop3 #FFF3E0 {
    rectangle "Download and Delete" as pop_model #FFE0B2
    
    note as pop_n
        * Downloads to device
        * Deletes from server (default)
        * Single device use
        * Offline access
        * Simple protocol
        
        Commands:
        USER, PASS, STAT
        LIST, RETR, DELE, QUIT
    end note
}

rectangle "IMAP\nPort 143 (993 TLS)" as imap #E3F2FD {
    rectangle "Sync with Server" as imap_model #BBDEFB
    
    note as imap_n
        * Keeps mail on server
        * Syncs across devices
        * Server-side folders
        * Partial download (headers)
        * Search on server
        
        Commands:
        LOGIN, SELECT, FETCH
        SEARCH, STORE, EXPUNGE
    end note
}

note as N1
    | Feature | POP3 | IMAP |
    | Storage | Local | Server |
    | Devices | Single | Multiple |
    | Bandwidth | High (full DL) | Low (sync) |
    | Offline | Yes | Limited |
    | Complexity | Simple | Complex |
end note

